import { ExternalLink } from './external-link';

describe('ExternalLink', () => {
  it('should create an instance', () => {
    const directive = new ExternalLink();
    expect(directive).toBeTruthy();
  });
});
