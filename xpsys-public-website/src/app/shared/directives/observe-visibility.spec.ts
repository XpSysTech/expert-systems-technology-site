import { ObserveVisibility } from './observe-visibility';

describe('ObserveVisibility', () => {
  it('should create an instance', () => {
    const directive = new ObserveVisibility();
    expect(directive).toBeTruthy();
  });
});
